"""Materials catalog JSON import pipeline.

See `planning/features/materials-catalog-import-export/` for the
product contract. The pipeline is backend-owned end-to-end so a
future MCP / CLI caller can reuse the same `preview` + `commit`
endpoints the browser uses.
"""

# Table Views

Headless feature package for persisted project table view state. It has
no route or component surface; table-facing UI is owned by
`shared/ui/data-table` and feature adapters consume these hooks.

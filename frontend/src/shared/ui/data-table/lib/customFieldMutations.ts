// Typed builders for `FieldSchemaMutation` payloads (plan-15 P2.4).
//
// Every browser callsite that needs to construct a schema mutation
// for the `WriteOp.schemaMutation` pipeline goes through one of
// these builders so the wire format is constructed in exactly one
// place. P2.4 fills the builders in; P2.0 places the module so
// follow-up renames stay surgical.

export {};
